package repository;

import domain.Award;
import domain.AwardId;
import java.util.List;
import java.util.Optional;

public interface AwardRepository {

    Award save(Award award);

    Optional<Award> findById(AwardId id);

    List<Award> findAll();

    void deleteById(AwardId id);
}
